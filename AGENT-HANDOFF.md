# WoodTek ERP — Handoff for a New Chat / Agent Session

_Last updated 2026-09-17. Read this first. The owner is NOT a developer — give exact copy-paste commands._

## What this project is
- Next.js **16.2.6** (standalone output) + PostgreSQL factory ERP ("WoodTek ERP – Furniture Service Center").
- Runs on a **Windows 10 PC**, folder `C:\woodtek-erp\woodtek-erp`; serves `0.0.0.0:3000`; LAN URL `http://192.168.220.179:3000`.
- Roles: Manager (Claude Aredjian), Machine Operator (maroun rahme), Sales Coordinator, QA & Dispatch, Technician.

## Source of truth
- **Private GitHub repo: `https://github.com/claudearedjian-cloud/woodtek-erp` (branch `main`).**
- The PC folder is a clone of it. Before any git work on the PC, check `git remote -v` points there.
- Other repos (`erp`, `system2`, `executable`) are old/other-session — **never** push or reset WoodTek code from them.
- To give an agent write access: create a **fresh fine-grained PAT** (only `woodtek-erp`, permission *Contents: Read and write*, short expiry), paste it in chat, revoke afterwards. Chat attachments accept images/text/md/json/pdf — **not .zip** (workaround: `copy x.zip x.txt` then attach the .txt).

## Deploy ritual (every change)
Agent commits + pushes to `main`; then on the PC, **in this exact order**:
```
git pull
node scripts\build-prod.cjs
schtasks /End /TN "\WoodTek ERP"
schtasks /Run /TN "\WoodTek ERP"
```
- **Never build while the task is running** → `EBUSY ... rmdir .next\standalone`.
- The task runs **as SYSTEM** → `taskkill` is always *Access denied*; only `schtasks /End` / `/Run` control it.

## Runtime architecture
- Scheduled task `\WoodTek ERP` (runs at logon) → `start-woodtek-prod-silent.bat` → `node start-prod.cjs --no-browser` → `node .next/standalone/server.js`. Logs: `logs\woodtek.log`.
- `start-prod.cjs` is the **v2 watchdog**: restarts real crashes (max 6/min), on `EADDRINUSE` it does NOT loop — it prints the owning PID and defers; health probe trusts only its own child process.
- `start-woodtek-prod.bat` = interactive launcher; if the task already serves port 3000 it prints "already in use … not starting a second copy" (by design — close the window).
- `next.config.ts` MUST keep `output: "standalone"` and `serverExternalPackages: ["bcryptjs","pg","@types/bcryptjs"]` — without it the build produces no `.next/standalone` and the server FATALs.
- `.env` (DATABASE_URL, AUTH_SECRET, …) is **local only**: never commit, never send to chats.

## Backups
- Task `\WoodTek ERP Backup` daily 02:30 → `backup\woodtek_factory_*.dump` (also stored in the private repo).
- `woodtek-snapshot.bat` → `backups\snapshot-DATE.zip` (source+configs+.env) before manual tinkering.
- `RUNBOOK.txt` at repo root = one-page ops guide.

## Role / UI rules implemented 2026-09-07/08
- Access matrix: `src/lib/moduleAccess.ts`; enforced by `src/components/Sidebar.tsx` (rendering) and `src/app/page.tsx` (tab guard bounces to first allowed module).
- **Machine Operator** sees only: Operator Station Mode, Scrap & Rework, Workforce & Shifts (+ Auto Workflow Engine card). *Active Role Persona* panel hidden; "Switch profile (PIN required)" kept.
- **Manager**: Routing Recipes, Downtime Log, Workforce & Shifts, PIMS Import, Shop Floor Monitor are nested **under General Settings** (indented sub-menu).
- All other roles unchanged.
- **Menu Designer** (added 2026-09-08): Manager-only top-level tab (`designer` module) to rename/reorder/regroup items and toggle per-role visibility. Stored as JSON at `<project>/data/menu-config.json` (env `WOODTEK_DATA_DIR` set by start-prod.cjs; API `/api/menu-config`, PUT/DELETE require `users:manage`). Defaults live in `src/lib/menuConfig.ts` (`MENU_REGISTRY`); `resolveMenu()` merges defaults + config per role; Sidebar renders its output. Reset button deletes the file.

- **Custom Roles** (added 2026-09-08): Manager can add/remove roles in Settings > Users > "Manage roles" (panel inside the add/edit-user form). A custom role is a **named alias of a built-in role** ("inherits ..."): it gets that role's full permission set, module visibility and data scoping. Storage: `<project>/data/roles-config.json` (`{version:1,roles:[{name,base}]}`, API `/api/roles`, PUT requires `users:manage`; removing a role still assigned to users is refused with 400). Registry: `registerCustomRoles/allRoles/baseRoleOf/getCustomRoles` in `src/lib/permissions.ts` (client-safe); server reads the file via `src/lib/rolesConfig.server.ts` (`ensureRolesRegistered()` — called from auth, users/menu-config/roles routes). **Session carries the BASE role** (`getSessionUser`/login POST resolve via `baseRoleOf`) so all ~40 legacy `role === "..."` checks keep working untouched; `displayRole` carries the custom name for UI (sidebar, profile). Menu Designer shows a column per custom role (overrides keyed by custom name; visibility defaults via base). Client registry filled at bootstrap (`page.tsx` fetches `/api/roles`). Built-in roles cannot be removed or shadowed; name <=30 chars, max 20 custom roles.

- **Machine Categories + Auto-assign** (added 2026-09-08): managers/technicians edit category names (add/remove) via "Manage categories" inside the New Order modal (also drives the Machine Type dropdown in Machines view and step categories in Recipe Manager). Storage: `<project>/data/machine-categories.json` (`{version:1,categories:[names]}`, API `/api/machine-categories`, GET open — seeds from distinct `machines.category` when no file; PUT requires `machines:write`, refuses to drop categories still used by machines). Membership = `machines.category` column (no migration). **Order routing steps now have Manual/Auto toggle**: Manual = exact machine (as before); Auto (default for new steps and recipe loads) = pick a category, server assigns the freest machine at creation (`pickFreestMachine` in `/api/orders` POST: exact→includes category match, skips Maintenance/Offline, least open ops (Pending/Ready/In Progress), active downtime pushed to back; helpers `sanitizeCategories`/`chooseFreestMachine` in `src/lib/machineCategories.ts`). Recipes (`templateId`) and the default 2-step path also use it. render-test covers the pure helpers (59 checks).

- **Editable Project Categories** (added 2026-09-08): the "Project Category" dropdown on the New Order form is Manager-editable ("Add / remove project categories" under the select). Storage: `<project>/data/project-types.json` (`{version:1,types:[names]}`, API `/api/project-types`, GET open — defaults (`DEFAULT_PROJECT_TYPES` in `src/lib/projectTypes.ts`) merged with distinct `orders.projectType` when no file; PUT requires `users:manage`, refuses to remove types still used by orders). Dropdown also always includes values existing orders use, so nothing disappears from history.

- **Warehouse & BOM** (added 2026-09-08): BOM lines can be entered directly on the New Order form ("Materials / BOM" section: inventory item + qty; written to the existing `orderMaterials` table with unit costs — same table as the order-detail BOM tab, stock still reserved/consumed by the existing materials engine). New top-level **Warehouse & BOM** tab (`warehouse` module, granted to Manager + QA & Dispatch): `src/components/WarehouseView.tsx` lists every open order (not Completed/On Hold/Cancelled) with its BOM lines; warehouse staff move lines Requested → Prepared → Delivered and pick a target machine from the order's operations. API `/api/bom` (GET orders:read; PUT requires inventory:write OR orders:write). Fulfilment status has no DB column (no migrations): JSON overlay `data/bom-status.json` keyed by allocation id (`src/lib/bomStatus.server.ts`). **Warehouse Supervisor** = custom role inheriting **QA & Dispatch** (create in Settings → Users → Manage roles). render-test: 63 checks. V2: custom roles may carry a `modules` allowlist (role editor in Settings > Users > Manage roles: pencil per role or checkboxes on add; empty = full base access) which REPLACES base screens in canAccessModule/listModulesForRole (page guard uses displayRole); BOM lines auto-target the FIRST machine of the routing (overlay set at creation), warehouse staff see it read-only and only `users:manage` (Manager) can re-route (PUT /api/bom). V3: operator station shows each order's BOM with line statuses and big RECEIVED / NOT RECEIVED confirmation buttons (PUT /api/bom {orderId, received}, allowed via inventory:write which operators have); the confirmation appears as chips on the Warehouse board order headers so manager/warehouse see who is waiting. V4: Dispatch queue on the Dispatch Schedule view: Completed orders appear with a stage pipeline — project categories whose name contains 'service' go straight to Awaiting delivery, everything else follows Cleaning -> QC -> Packing -> Awaiting delivery -> Delivered; stage advancement via PUT /api/dispatch (quality:write OR orders:write OR inventory:write); stages stored in data/dispatch-status.json (src/lib/dispatch.ts pure helpers). render-test 68 checks. V5: Delivery closes the loop — "Mark delivered" in the Dispatch queue sets orders.status="Delivered" in the DB (PUT /api/dispatch); Orders & Routing gains a Delivered filter chip, a teal Delivered kanban column + badge, and live "Dispatch: <stage>" chips on completed orders (polls /api/dispatch every 15 s); Delivered orders leave every open-work query (WIP materials, BOM board, customer active counts) and count as Completed in dashboard/reports stats; order materials lock treats Delivered as terminal. render-test 68 checks.
- **Dashboard rearrange** (added 2026-09-09): the OEE & Plant Performance panel moved off the Executive Dashboard into its own **Plant Performance** tab (`plant` module, `src/components/PlantView.tsx`, Gauge icon, granted to Manager / Sales Coordinator / QA & Dispatch / Technician; fetches /api/dashboard?window= itself). The WIP Pipeline Value and Machine Utilization KPI tiles were removed from the dashboard top. The dashboard now opens with an **Orders by status** quick bar (In Production / Pending / Quality Review / Completed / Delivered / On Hold, live counts from orderStatusDistribution) — each button jumps to Orders & Routing pre-filtered (page.tsx `navigateWithStatus` + OrdersView `presetStatus` prop). orderStatusDistribution now reports Completed and Delivered separately (+Cancelled). Order numbers are rendered bigger/bolder (font-mono font-black amber) with the client name next to them on the dashboard feed, Orders list + kanban, WIP board (running card + queue chips + order cards), Warehouse board, Dispatch queue, Operator Station pill and Machines running card; /api/dashboard activeShopJobs and /api/wip currentJob now carry customerName.
- **CMMS full access** (added 2026-09-09): Power & Generator telemetry cards now carry **Edit Asset** (full field modal: tag/name/brand/type/site/serial/series/year/kVA/meter/interval/last-service/status/criticality/notes → POST /api/cmms with id) and **Delete** (confirm → DELETE /api/cmms?assetId=) buttons for Manager/Technician/QA & Dispatch; canManage in CmmsView + AssetDetailView extended to QA & Dispatch; asset DELETE now guarded by cmms:write (was machines:write, which locked out QA & Dispatch). Created the MISSING `src/app/api/cmms/logs/route.ts` — CmmsView and AssetDetailView were POSTing maintenance events to a 404; POST inserts the log (resets service meter + moves hour meter forward when resetService), DELETE ?logId= removes an entry (trash icon per log row in AssetDetailView).
- **Project categories deletable** (added 2026-09-09): OrdersView used to re-merge DEFAULT_PROJECT_TYPES into the list on load and after every save, so deleted default categories (Custom Kitchens etc.) instantly reappeared. Now the client trusts the stored list verbatim (GET /api/project-types already seeds defaults+in-use types when no data/project-types.json exists). The server still refuses (400, message shown in the manager box) to remove a category that existing orders carry — by design; force-delete can be added if the boss asks.
- **Sequential order numbers** (added 2026-09-09): new orders are numbered **PO-0001/2026** — 4-digit sequential counter scoped to the calendar year (`src/lib/orderNumbers.server.ts` nextOrderNumber(), used by POST /api/orders when no explicit orderNumber is supplied; the old fallback was a random ORD-2026-XXXX with a hardcoded year). The counter finds MAX(PO-####/<year>) in the orders table, so it auto-resets to 0001 on Jan 1 with zero maintenance. Legacy ORD-…/PIMS-… numbers are untouched and skipped by the pattern.
- **New Order modal = 3 tabs** (added 2026-09-09): the crowded one-scroll form is now a wizard — `1 · Order Details` (client, project category + manager, title, priority, value, due date), `2 · Machine Routing` (recipes + step chain), `3 · Materials (n)` (BOM). Back/Next buttons in the footer; Schedule & Route Order submits from any tab; the form onSubmit jumps to the offending tab when validation fails (missing client/title → Details, no steps → Routing); tab resets to 1 whenever the modal opens.
- **Auto-plan dispatch slots** (added 2026-09-09): the "Needs a slot" panel on Dispatch Schedule has a big **Auto-plan N operations** button (Manager/Sales). `POST /api/operations/auto-schedule` (operations:update-status) assigns slots to every Pending/Ready operation without one: orders sorted Urgent→High→Normal→Low then due date; inside an order operations follow stepOrder and never start before the previous step's end; machines are never double-booked (existing bookings respected, incl. slots assigned in the same run) and Maintenance/Offline machines are skipped; operations without a machine go to whichever usable machine can start them earliest. Manual "Plan slot" modal remains for fine-tuning; response reports planned/skipped counts.
- **Order Status report fixed** (added 2026-09-10): the report generated fine server-side (byStatus counts + full order list) and PDF/CSV exporters existed, but ReportContent had NO "Order Status" branch, so the on-screen preview fell through to "Report type not recognized." — added the renderer: 5 status stat cards (Pending / In Production / Quality Review / Completed / On Hold, Completed includes Delivered) + full orders table (order #, title, status chip, progress, due date, value).
- **Clients & Architects overhaul** (added 2026-09-10): CustomersView rewritten — top-bar **search** (company/contact/email/phone/address, live "Showing X of Y", clear button, empty state); clicking any client card opens a **Client File modal** with two tabs: **Profile** (all info incl. credit limit, balance, notes; **Edit Information** inline form → PATCH /api/customers/:id; **Delete Client** → DELETE, server still blocks deletion while orders exist) and **Orders (n)** (full order history from GET /api/customers/:id — big amber order numbers, status chips, progress, due date, value; row click jumps to the order workflow). Card trash icon still works with stopPropagation.
- **Top-3 improvements** (added 2026-09-10): (1) **Alerts bell** on the dashboard status bar — polls new `GET /api/alerts` (orders:read) every 20 s: overdue open orders, operators' "materials NOT received" flags (bom-status received map), CMMS assets past their service interval, inventory at/below reorder level; each alert click navigates to its tab (orders/warehouse/cmms/inventory). (2) **PIN reset**: Settings user edit form now always shows the PIN field ("Reset PIN (leave empty to keep the current PIN)") — the settings POST already hashed+stored it; fixes the long-standing "cannot reset PINs" gap. (3) **Proof of delivery**: "Mark delivered" in the Dispatch queue opens a prompt (Received by* + notes) → PUT /api/dispatch stores `proof {receivedBy, deliveredAt, notes}` in dispatch-status.json and GET returns it; delivered rows show "✓ Delivered to X · date · notes".
- **Money bundle** (added 2026-09-10): (1) **Quotation PDF** — button on the order workflow header builds a branded client quote via jsPDF (WOODTEK band, Bill-To, meta, BOM materials table with amounts when costs are visible, production-steps table, Materials + Production split, TOTAL QUOTED = order totalValue, terms footer; saved as Quotation-<orderNumber>.pdf). (2) **Order Profitability report** (Reports tab, respects the date range): per order quoted vs material cost (orderMaterials qty×costPerUnit) + labor cost (actual-or-estimated minutes × machine hourlyCost) → profit + margin %, plus per-client rollup and totals KPIs; on-screen tables + PDF + CSV. (3) **Client ledger / balance tracking** — Ledger tab in the client file modal (Clients & Architects): record invoices and payments (type/amount/reference), live summary (Invoiced, Paid, Outstanding, Available Credit with a usage bar vs creditLimit), delete entries; stored in data/client-ledger.json (no migration) and customers.currentBalance is re-synced after every change.
- **QR order stickers** (added 2026-09-10): new dep `qrcode`. Order workflow header has a **QR Sticker** button → modal with the QR (encodes `<origin>/?order=<id>`) + order number/title + **Print sticker** (opens a clean print window). `page.tsx` honors `/?order=<id>` deep links (opens the order tab after sign-in — phone scans work). Operator Station has a **scan box** above the queue: USB scanner or typed order number / sticker URL → finds the matching job card on that station, scrolls to it and rings it in sky blue for 5 s; unknown scans show "No job from … on this station".
- **Auto In-Production + bigger order identifiers** (added 2026-09-10): when an operation is set to In Progress (PATCH /api/operations/:id) and the parent order is still Quoted or Deposit Paid, the order status auto-flips to **In Production** inside the same transaction. Order number + client are now visually dominant everywhere: order number bumped one size up (list/kanban/WIP/station chip/detail header/schedule/quality), client name white extrabold, station cards gained a bold amber **Client:** line (customerName/customerCompany now joined in `listOperationsForUser`), order detail header shows Client at text-sm white.
- **Floor Supervisor role + reception control** (added 2026-09-10): new BUILT-IN role "Floor Supervisor" (ROLES + MATRIX + MODULES_BY_ROLE: dashboard/orders/station/wip/warehouse/inventory/quality/downtime/workforce/plant; lands on station tab at login). New action `bom:receive` — Manager + Floor Supervisor only. Material reception is now three-state (Received / Not Received / Declined) in data/bom-status.json (BomReceivedEntry.state, back-compatible with old boolean entries); station UI shows ✓ APPROVE RECEPTION / ✗ NOT RECEIVED / ⛔ DECLINE buttons ONLY to Floor Supervisor/Manager (check: role or displayRole === "Floor Supervisor"), operators get a read-only BOM + "Waiting for Floor Supervisor" note. PUT /api/bom reception branch returns 403 for everyone else. HARD GATE: PATCH /api/operations/:id refuses to start (409) any operation of an order that has BOM lines until reception state is Received. WarehouseView badges updated (Reception approved / NOT received / DECLINED).
- **Editable role screens** (added 2026-09-10): Settings > Manage roles now has a pencil on EVERY built-in role except Manager (Manager is locked to full access to prevent lock-out). Picking screens writes a per-role override into data/roles-config.json (`overrides` map, alongside `roles`); "reset" removes the entry and restores defaults. Resolution order in moduleAccess: custom role's explicit modules -> override for that exact role name -> override for the custom role's base -> built-in defaults. Server: GET/PUT /api/roles carry `overrides`; PUT drops unknown module ids, empty lists, unknown role names, and any "Manager" key. Client bootstrap registers overrides from /api/roles so sidebar + page guard agree immediately.
- **Floor Supervisor data scoping** (fixed 2026-09-11): new built-in roles MUST be added to BOTH layers: permissions/menu AND dataAccess scoping. Floor Supervisor was missing from `allowedOrderIdsSubquery` (fell to default-deny sentinel id=-1 → 0 orders/ops/WIP) and from `isFloorRole` (financial redaction). Now: full order/operation visibility like Manager, with quote values redacted like other floor roles.
- **Reception approval gated on warehouse delivery** (added 2026-09-11): PUT /api/bom with state "Received" now 409s unless EVERY orderMaterials line of the order is Delivered in the bom-status overlay (missing entry = Requested). Station UI disables the ✓ APPROVE RECEPTION button while any line is not Delivered + shows a sky-blue "warehouse must deliver first" note; NOT RECEIVED / DECLINE stay always available. confirmReceived surfaces server 409 text via setActionError (6s).
- **Material Reception board** (added 2026-09-11): new module id `reception` ("Material Reception", badge RX) — Floor Supervisor's LOGIN LANDING tab and also granted to Manager. New component src/components/FloorReceptionView.tsx: fetches /api/bom (open orders + line statuses + receivedState), KPI cards (awaiting/approved/rejected), filters, per-order card with big order number + client + delivery progress (X/Y delivered) and the three decision buttons (✓ APPROVE locked until all lines Delivered; ✗ NOT RECEIVED; ⛔ DECLINE); 10s auto-refresh; server errors shown via banner. Wiring checklist used for a NEW TAB: permissions KNOWN_MODULE_IDS -> moduleAccess ModuleId union + MODULE_LABELS + MODULES_BY_ROLE entries -> menuConfig MENU_REGISTRY -> Sidebar ICONS (+lucide import) -> page.tsx import + default-tab + render.
- **FS machine choice** (added 2026-09-11): new action `operations:assign-machine` (Manager + Floor Supervisor). When a step's machine category has 2+ Active/In-Use machines (BEAM-01/BEAM-02...), the Reception board shows a "Machine assignment — your decision" row per eligible step (status Pending/Ready): candidate code buttons, current one green; locked (🔒 after approval) until reception approved. Server (PATCH /api/operations/:id): machine-only PATCH from an assign-machine role bypasses canUserUpdateOperation; inside the tx it enforces same-category, machine Active/In-Use, status not started, and reception approved (409s otherwise). GET /api/bom now returns per-order `operations` with `candidates` built from machines.category.
- **Decision buttons hide after approval** (added 2026-09-11): once an order's reception is Approved (received === true), the ✓/✗/⛔ trio disappears from both FloorReceptionView cards and the station BOM box (card keeps its RECEIVED ✓ badge + machine-assignment section; Declined/Not-received states keep the trio so a wrong call can be corrected).
- **Warehouse Supervisor built-in** (added 2026-09-11): the old custom role could inherit a base without inventory:write -> Prepare/Send buttons vanished (WarehouseView gates them on inventory:write/orders:write). Now a real built-in role: BASE_READ + inventory:write + shifts/attendance + wip:read; modules warehouse+inventory+workforce; lands on Warehouse tab. A legacy custom role with the same name is superseded automatically (registerCustomRoles drops name collisions with built-ins).
- **Profile-switch tab bounce** (fixed 2026-09-14): after switching profiles, activeTab could stay on the previous user's tab (e.g. "reception"); tabToModule didn't know "reception" (returned null) and the guard only bounced when m was non-null -> returning user stranded on a foreign screen ("screen disappears"). Now: "reception" is in the tab->module map and the guard bounces on `!m || !canAccessModule(...)` to the role's first allowed module.
- **Profile-switch station stranding** (fixed 2026-09-14, second pass after user screenshot): returning to a non-operator role left activeTab="station" because handleAuthenticated only SET tabs for the three landing roles and the guard bounced "operator" to listModulesForRole[0] which for Machine-Operator-based customs is "operator" again (station). Now: (1) handleAuthenticated resets to "dashboard" when a non-landing role authenticates while on station/reception; (2) guard bounce uses a LANDING map (operator->station, FS->reception, WS->warehouse) before falling back to first module. Screenshot symptom was: WS persona seeing Operator Touchscreen Mode + empty sidebar ("Sign in to continue" card).
- **Shop Floor Monitor Manager CRUD** (added 2026-09-14): MachinesView got currentUser prop; `canManage = role === "Manager"` gates Register Machine (blue +), per-card Pencil **Edit** (opens the same modal pre-filled, PATCH /api/machines/:id) and Trash delete. API already had POST/PATCH/DELETE with machines:write. Non-managers (e.g. QA & Dispatch who can view the monitor) see no add/edit/delete buttons (SSR-verified).
- **Multi-operator machines** (added 2026-09-14): crew per machine lives in data/machine-operators.json overlay (src/lib/machineOperators.server.ts; machines.assigned_operator_id mirrors crew[0] for legacy logic). Manager UI: card shows crew chips with x-remove + "+ assign operator" select; Register/Edit modal has a crew editor (first = primary). GET /api/machines returns assignedOperatorIds. Auth widened: canUserUpdateOperation + Machine-Operator order scoping accept any crew member (OR inArray machines). OperatorStationView myMachineId/isMine use assignedOperatorIds. DELETE machine cleans the overlay entry.
- **DB backup/restore buttons** (added 2026-09-14): Header top-right, Manager-only: DatabaseBackup icon (confirm -> POST /api/admin/dbtools {action:backup} -> pg_dump plain SQL into backup/db-dumps/woodtek-db-<ts>.sql) and ArchiveRestore icon (dropdown lists dumps from GET; RESTORE per file -> confirm -> kills other PG backends then psql -v ON_ERROR_STOP=1 --single-transaction; app reloads on success). Route is users:manage-gated; file names validated (no separators/..); pg tools located via WOODTEK_PG_BIN, C:\Program Files\PostgreSQL\*\bin, then PATH. lucide has NO DatabaseRestore icon in this version — use ArchiveRestore.
- **Animated DB confirm modal** (added 2026-09-14): Header native confirm() replaced with a styled modal (dbpop/dbstripe/dbpulse keyframes via inline <style>): confirm question ("Are you sure you want to backup/restore the database…"), then a WORKING phase that cannot be closed (spinner + animated stripe bar + pulsing hint) until the fetch finishes, then green check auto-close (backup) or reload (restore), or red error with Close.
- **dbtools restore fix** (2026-09-14, from user screenshot): plain pg_dump without --clean made restore fail on a live DB ("relation assets already exists"); and process.cwd() in prod is .next/standalone so dumps lived inside the build tree (wiped on rebuild). Now: pg_dump uses --clean --if-exists; dumpsDir = WOODTEK_DB_BACKUP_DIR || (win32: C:\WoodTekBackups\db-dumps) || cwd/backup/db-dumps. Old pre-fix dumps cannot restore over a populated DB — take a new backup after updating.
- **Global search dropdown** (fixed 2026-09-14): the top bar search previously only filtered lists inside Orders/Schedule/Gantt/CMMS/Reports — on other screens it appeared dead. Header now receives orders/customers/machines + onOpenOrder/onNavigate from page.tsx and shows a live results dropdown (>=2 chars, focused): Orders section (click opens the order), Clients (jumps to customers tab), Machines (jumps to Shop Floor Monitor). Per-view list filtering unchanged.
- **Menu Designer capacity upgrade** (added 2026-09-14): three owner-requested expansions. (1) **Custom named sections** — the placement dropdown now offers "➕ New section…" (browser prompt, name <=30 chars); any item (registry OR custom) can live in a custom section; `resolveMenu` returns `sections: {name, items}[]` in first-appearance order and the Sidebar renders each as its own header block (amber label + divider) after the top items; a section header only shows for roles that can see >=1 item inside; empty sections vanish. (2) **Custom menu entries** — blue "Add menu item" button on the tab (rows prepend at top, sky-blue bordered, trash-deletable): each custom entry (`custom-<base36>` id) either opens an existing screen (dropdown of registry tabs; sidebar icon = target's icon, active-highlight when that tab is open) or opens a website in a new tab (http/https only — `javascript:` etc. collapse to a safe tab both in `sanitizeMenuConfig` AND in `resolveMenu` via `safeCustomNav`, so even hand-edited JSON can't inject URLs; `<a target="_blank" rel="noopener noreferrer">`). Custom entries default visible to Manager only, per-role checkboxes as usual; "Reset defaults" removes everything. (3) **Limits raised**: label 40→60 (`LABEL_MAX`), badge 10→20 (`BADGE_MAX`), group name <=30 (`GROUP_MAX`) — clamped in the inputs, `resolveMenu` and `sanitizeMenuConfig`; label input widened. No DB/config-format break: same `data/menu-config.json`, `MenuConfigItem` gains optional `custom/kind/target`, `group` widened to string. render-test 68→82 checks (section resolution/order, clamps, link safety, operator default-hide, SSR anchor + header).
- **Landing tabs + icon picker + Ctrl+K search** (added 2026-09-14, second bundle): (1) **Per-role landing screen** — new "Landing screen per role" card in Menu Designer (emerald): a select per role (factory default = built-in map operator→station / FS→reception / WS→warehouse) listing only tabs that role may open; stored as `landing: {role: tabId}` in menu-config.json (sanitize: unknown roles dropped, tabs the role cannot open dropped via canAccessModule; `getLandingTab()` re-validates at read time so hand-edited JSON can't strand anyone). page.tsx `handleAuthenticated` AND the guard-bounce effect check displayRole first then base role, then fall back to the built-in LANDING map; guard effect now also depends on menuConfig. (2) **Icon picker** — `MENU_ICON_KEYS` allowlist in menuConfig.ts (59 keys, server-validated), new client-only `src/lib/menuIcons.ts` maps keys → lucide components (`MENU_ICON_CHOICES`, `iconChoice`); per-item "Auto icon" select on every designer row (Auto = the screen's own icon / ExternalLink for links) + "Section header icons" card when custom sections exist; Sidebar's ICONS map extended with the palette and precedence is `item.icon ?? target/id icon ?? Layers`; render-test asserts the key↔component lists stay in sync. (3) **Ctrl+K / "/" search shortcut** — page.tsx window keydown listener focuses+selects the top-bar search (`id="woodtek-global-search"`; "/" ignored while typing in inputs; Escape blurs); Header input's Escape clears the query first, kbd badge now reads "Ctrl K". render-test 82→97 checks.
- **Clone order + packing-slip PDF + partial BOM delivery + downtime Pareto** (added 2026-09-14, third bundle): (1) **Clone order** — green "Clone" button in the order workflow header (Manager/Sales Coordinator only, next to Quotation PDF) builds a seed {customerId, title+" (copy)", projectType, priority, totalValue, dueDate, notes, steps (operationName/machineId/machineCategory/estimatedMinutes, auto=false), bom (itemId/qty)} from the loaded order detail; page.tsx holds it as `cloneSeed`, jumps to the orders tab and opens the New Order wizard; OrdersView consumes the seed in an effect (prefills all three tabs, appends the cloned project type to the managed list if missing, clears recipe, then calls onCloneConsumed). Nothing is auto-submitted — the user reviews and presses Schedule & Route Order. (2) **Packing-slip PDF** — GET /api/dispatch now returns each queue order's `materials` (itemName/SKU/unit/qty via orderMaterials⨝inventoryItems); ScheduleView's dispatch queue rows at Packing/Awaiting/Delivered carry a "Packing slip" button (jsPDF + jspdf-autotable, already a dependency): WOODTEK band, big order number, client/category/due, materials table with a "Checked" column, Packed-by/Received-by signature lines; saves PackingSlip-<orderNumber>.pdf. (3) **Partial BOM delivery** — BomStatusEntry gains `deliveredQty` (JSON overlay, no migration); PUT /api/bom accepts `deliverQty` (clamped 0..lineQty server-side against orderMaterials.quantityUsed): status stays Prepared with a tally until the full quantity is sent, "Delivered" always stores the full qty, Undo to Requested clears it; reception gate UNCHANGED (all lines must be status Delivered). New pure lib src/lib/bomDelivery.ts (clampDeliveredQty/sentQty/statusAfterPartial); WarehouseView shows "x/N sent" chips + "Partial…" button (prompt) on Prepared lines with qty>1. (4) **Downtime Pareto** — new pure lib src/lib/downtimeReasons.ts (DOWNTIME_REASON_CODES = the existing 7 codes, normalizeReason buckets legacy free text by keywords, buildPareto with day-window + open-event running time + A/B bands where the cause crossing the 80% line is still A, paretoToCsv); DowntimeView bottom panel: period select (7/30/90/all), bar rows (reason, minutes, events, share · cumulative, A/B badge), CSV download; replaced the local reasons array with the shared lib. render-test 97→119 checks.
- **Client statement PDF + daily digest** (added 2026-09-14, fourth bundle): (1) **Statement PDF** — new "Statement PDF" button at the top of the client-file Ledger tab (CustomersView, disabled while the client has no entries): jsPDF + jspdf-autotable — WOODTEK band, client company + contact + address, a 5-box summary row (Invoiced / Paid / Outstanding / Credit limit / Available), then the full chronological entry table (Date, Type, Reference, Invoiced, Paid, running Balance) with an amber TOTAL footer row; saves Statement-<Company>.pdf. New pure lib src/lib/clientStatement.ts: buildStatement() sorts entries by timestamp (hand-edited JSON can't scramble order), builds running-balance rows, invoices add / payments subtract, amounts clamp positive and round to 2dp. (2) **Daily digest (morning brief)** — new GET /api/digest (orders:read, same surfaces as the alerts bell): overdue open orders with days late, due within 7 days, operator materials flags (NOT RECEIVED / DECLINED on open orders via bom-status received map), open orders with warehouse BOM lines not fully sent (pending/total per order via bom-status overlay), machines currently down (open downtime_events + running minutes), CMMS assets past service interval (same rule as alerts), completed orders awaiting delivery, low stock; all lists capped. Pure lib src/lib/digest.ts: digestToLines() renders section headers (no indent) + detail lines (2-space indent) with counts and an ellipsis cap line, digestTotalIssues() counts the 5 attention sections. DashboardView gained a currentUser prop (page.tsx passes it) and a "Morning digest" card above the status quick bar — Manager + Sales Coordinator only — 8 click-through count tiles (each jumps to its tab: orders/orders/reception/warehouse/downtime/cmms/schedule/inventory) + "Print digest" button that renders the brief to a paged WOODTEK PDF (MorningDigest-<date>.pdf); card polls /api/digest every 60 s. render-test 119→134 checks (digest lines/caps/none-sections, statement running balance/sort/totals/clamps).
- **Audit log + DB dump retention + AR/FR labels** (added 2026-09-14, fifth bundle): (1) **Audit log** — new server lib src/lib/audit.server.ts: `logAudit(actor, action, entity, detail?, entityId?)` appends to data/audit-log.json (no migration), file capped at the newest 2000 entries (AUDIT_MAX), detail clamped to 300 chars, NEVER throws into the caller; `readAudit({q, limit})` newest-first with substring filter; `clearAudit()`. Instrumented routes (all log AFTER success): auth POST login ok + failed PIN/unknown-user, auth DELETE sign-out (reads session first), operations PATCH (status change / FS machine assign), orders POST create, orders/[id] PATCH (status flip vs field edit), orders/[id] DELETE, bom PUT (line moves incl. partial tallies + reception approve/decline/flag), dispatch PUT (stage + proof-of-delivery), users POST, settings POST (user create / profile update with PIN-reset + role note), dbtools backup/restore, menu-config PUT, roles PUT. New GET/DELETE /api/audit (users:manage). SettingsView (Manager-only) gained an "Audit log — who did what" panel: Show-trail button, filter box + Search/All, table (When / Who (role) / action chip / detail), CSV export, Clear (confirm); panel sits above the add/edit modal, tab-level per house rules. (2) **DB dump retention** — dbtools keeps the newest WOODTEK_DB_BACKUP_KEEP dumps (default 30, floor 3): pruneDumps() runs after every successful backup (locked files are skipped, never fatal), GET returns `keep`; POST response carries `pruned` count which lands in the audit entry. (3) **AR/FR labels** — new pure lib src/lib/i18n.ts: EN/العربية/Français dictionaries keyed by the EXACT English default string (~45 labels: all 21 menu items, sidebar chrome, top bar, sign-in screen); `tt(lang, english)` passes through anything unknown — so Menu Designer custom names are NEVER overwritten. page.tsx owns a `lang` state (localStorage `woodtek-lang`, per device, EN default) and passes it to Header (new Languages-icon dropdown EN/AR/FR), Sidebar (item labels + static section headers translated) and AuthGate (title, subtitle, PIN hint, sign-in button). NO RTL layout flip in this pass — Arabic text renders inside the existing LTR chrome (noted to the owner; a mirrored layout can be a later pass). render-test 134→149 checks (audit append/filter/clear/clamp on a real tmp dir, AR/FR key-set parity + passthrough of custom names, SSR Arabic sidebar).
- **FULL system backup/restore + auto-lock on idle** (added 2026-09-14, sixth bundle — closes backlog items 1 and 2 from the suggestions round): (1) **FULL backup** — dbtools gained `{action:"full-backup"}`: pg_dump (same flags as DB-only) into `<FULL dir>/woodtek-full-<stamp>/db.sql`, then a recursive copy of EVERY file in the data dir (WOODTEK_DATA_DIR || cwd/data — ledgers, bom-status, dispatch-status, client-ledger, audit-log, roles-config, menu-config, machine-operators…) into `data/`, plus manifest.txt; folder kept under `C:\WoodTekBackups\full` (WOODTEK_FULL_BACKUP_DIR override), pruned to keepCount() after each run. GET now returns `fulls: [{dir, sizeKb, dataFiles, at}]`. `{action:"full-restore", dir}`: name validated (woodtek-full- prefix, no separators/..), terminates other backends, psql --single-transaction the db.sql FIRST (on failure data files are untouched), then overlays the saved data files back (files only in today's data dir are left in place), audited. Header: third Manager button HardDriveDownload (sky) = full backup through the same animated modal (kind "full"); the restore dropdown now has two sections — FULL system entries ("RESTORE ALL", kind "full-restore", reloads on success) and Database only. Modal title/text/button per kind. (2) **Auto-lock on idle** — new pure lib src/lib/idle.ts (normalizeIdleMinutes — null/""/garbage -> default 15, NOT 0; idleState(mins, lastMs, nowMs) -> {warn, lock, remainingSec}, warning in the last 60s). page.tsx: pointer/key/wheel/touch listeners refresh lastActivityRef; a 5s timer (skips when signed out or auth modal open) locks via lockWorkspaceRef (latest lockWorkspace forwarded after definition) and drives a fixed top "auto-lock soon" banner (tap to cancel); minutes read from localStorage woodtek-idle-mins each tick so no cross-component wiring. SettingsView: "Auto-lock this device" panel for EVERY user (5/10/15/30/never), per-device, explains the 60s warning. i18n: banner string added to AR+FR dicts (parity kept). render-test 149→160 checks (normalize fallbacks incl. the null→0 trap, warn/lock/never transitions, storage read).
- **BOM kits + order-entry stock check + Operator Performance v2 + delivery manifest** (added 2026-09-14, seventh bundle): (1) **BOM kits** — reusable materials lists for the New Order wizard: data/bom-kits.json ({version:1,kits:[{name,items:[{itemId,qty}]}]}), API /api/bom-kits (GET orders:read; PUT create/replace + DELETE ?name= gated on inventory:write OR orders:write — same as the BOM board; both audited as bom.kit.save/delete). Pure lib src/lib/bomKits.ts: sanitizeKitName (<=40), sanitizeKitList (dedupe case-insensitive, merge duplicate itemIds by summing, qty positive integers, <=60 items/kit, <=50 kits), findKit, mergeBomKit (appends a kit onto the wizard draft, SUMMING quantities when an item is already listed). OrdersView materials tab: teal "Material kits" box — select to load (merges into the current BOM), trash to delete, "Save as kit" (prompt for name) next to Add material. (2) **Stock check at order entry** — same lib: stockShortfall(lines, itemsById) aggregates requested qty per item across ALL draft lines and compares with inventoryItems.stockQuantity; OrdersView shows an amber warning box (top offenders with needs/stock/missing) + a red −N chip per short row, live while typing. Unknown item ids are skipped (API rejects them at creation). (3) **Operator Performance v2** — the existing report was upgraded (NOT duplicated): now respects the date range (completed ops filtered by endTime in [from, to]), per-operator table gains Estimated h + Avg Δ min/op (actual−estimated average; + takes longer, − faster) next to Actual h and efficiency; NEW machines section (per machine: ops, est vs actual h, avg min/op, efficiency) in the on-screen renderer, the PDF (second autoTable) and the CSV (MACHINE: rows appended); operators list now includes custom roles whose BASE is Machine Operator (users table stores custom names — resolve via baseRoleOf from permissions). (4) **Delivery manifest** — GET /api/dispatch now returns customerAddress + customerPhone; ScheduleView dispatch queue header has a "Print manifest" button: jsPDF driver sheet of every order at awaiting_delivery, sorted by address (same-area stops cluster), each stop a boxed card (numbered order number, client, address, phone, [ ] Delivered / [ ] Returned tick line, signature line), date + driver/vehicle lines in the header, footer note; saves DeliveryManifest-<date>.pdf; no-op with a notice when nothing is awaiting. render-test 160→170 checks (kit sanitize/merge/dupe handling, stock shortfall aggregation + gap math).

- **Quote follow-up alerts + AR/FR quotations** (added 2026-09-15, eighth bundle — closes backlog items 7 and 8): (1) **Quote follow-up alerts** — quotations waiting too long for a client answer now surface in BOTH existing alert surfaces (no new screen, no migration): GET /api/alerts emits an amber `quote` alert per order stuck in Quoted or Deposit Paid whose createdAt is at least WOODTEK_QUOTE_STALE_DAYS old (env override, default 7) — title `<order> quote is N days old`, detail client · title, tab "orders"; GET /api/digest gained a `staleQuotes` list (same rule, capped 12, oldest first: id/orderNumber/title/customerCompany/daysOld) rendered by digest.ts as a "STALE QUOTES - FOLLOW UP" section (detail line `PO-…/2026 · Client · Title · quote is N days old`) and counted by digestTotalIssues; DashboardView digest card gained an amber "Stale quotes" tile (same click-through to orders, first-line preview shows `· Nd old`). (2) **AR/FR quotations** — the client-facing Quotation PDF (OrderWorkflowDetail) is now trilingual: i18n.ts gained `QuoteStrings` + `QUOTE_STRINGS` {en, ar, fr} (23 keys: quotation, tagline, billTo, details, attn, date, due, category, reference, material, qty, unit, unitCost, amount, productionSteps, operation, station, estHours, materials, productionFull, totalQuoted, terms, generated); the single Quotation PDF button became a "Quotation PDF ▾" dropdown — English / Français build the same jsPDF quote with `qs.*` labels, العربية — طباعة opens an RTL HTML print window instead (browser text shaping renders Arabic correctly where jsPDF built-in fonts cannot; WOODTEK dark band + amber accents, same content blocks and totals, esc() for &<>, window auto-prints after 700 ms; blocked pop-ups surface the existing action-error strip: "Allow pop-ups…"). render-test 170→176 (stale-quote section/days-old/total counts + QUOTE_STRINGS EN/AR/FR key parity and translated titles). Repo hygiene: `compiled/` added to .gitignore (stray new compiled libs can no longer ride into commits; already-tracked compiled files still update) and the leftover compiled/lib/bomKits.js removed from the index.

- **Wall board TV + order archive + packing QC checklist + delivery photos** (added 2026-09-15, ninth bundle — closes backlog items 9-12, the whole suggestions list): (1) **Wall board (TV mode)** — "Wall board" button on the WIP Board header opens a full-screen dark overlay (component src/components/WallBoardView.tsx, pure shaper src/lib/wallboard.ts) that reuses /api/wip, polls every 15 s with a 1 s clock tick: big machine tiles (state-colored; Running shows order number XXL amber, client, operation, ticking elapsed + queue depth/minutes; Down/Idle states big), active-orders strip with progress bars, KPI chips, live clock/date; double-click or the ⛶ button requests browser fullscreen, Esc/X exits. Operators' screens untouched. (2) **Order archive** — Archive/Unarchive per order row (list view, Manager + Sales Coordinator; stopPropagation so the row click still opens) + gray "Archived" chip + an "Archived (N)" toggle in the Orders filter bar (ON = show ONLY archived orders; archived are hidden from list AND kanban otherwise). Storage data/order-archive.json {version:1, archived:[ids]} via GET/PUT /api/order-archive (orders:read / orders:write, audited order.archive/unarchive); pure helpers src/lib/orderArchive.ts (sanitizeArchivedIds dedupe+sort+cap 5000, withArchived). Nothing is deleted; reports/digest still see archived orders. (3) **Packing QC checklist** — Manager-editable template (SettingsView "Packing QC checklist" panel: edit/add/remove items, Save, Disable gate = empty template; data/packing-qc-template.json, MISSING file = factory default of 5 items) + per-order ticks (data/packing-qc.json) shown/edited from an inline "QC done/total" panel on Schedule dispatch cards (project flow, Packing/Awaiting stages; checkboxes gated canDispatch = quality:write||orders:write||inventory:write). HARD GATE: PUT /api/dispatch refuses packing→awaiting_delivery (409) until every item is ticked — the Next button shows "QC checklist first" and the error banner prints the server message; service orders skip Packing so they are never blocked. Routes: GET/PUT /api/packing-qc (template PUT = users:manage; ticks audited packing.qc); shared readers in src/lib/packingQc.server.ts; pure src/lib/packingQc.ts (sanitizeTemplate dedupe/≤12/≤120, normalizeChecks strict booleans, checklistComplete/Progress, DEFAULT_QC_TEMPLATE). (4) **Delivery photos** — "Photos" button on dispatch cards (Packing/Awaiting/Delivered) opens an inline panel: thumbnails from <data>/uploads/delivery/ served by GET /api/delivery-photos?file= (name strictly validated), Add photos = hidden multi-file input (accept image/*, phone camera works through the file picker), per-photo delete, by/date caption. POST formData (orderId + files[]) validates JPG/PNG/WebP ≤8 MB, max 12/order; metadata data/delivery-photos.json; DELETE removes file+meta; all writes audited delivery.photo.*. Pure src/lib/deliveryPhotos.ts (validatePhoto/photoExtFor/isSafeStoredPhotoName/sanitizePhotoMeta). render-test 176→198 (packingQc sanitize/normalize/complete/gate-off, archive clean/add/remove, photo validation + traversal-safe names, wallboard mapping incl. null job and empty payload).

- **Machine assignment fixes — Floor Supervisor locked out + wrong machines offered** (fixed 2026-09-15, tenth bundle, user-reported): (1) New helper `canAssignMachines(role)` in permissions.ts = `can(role,"operations:assign-machine") || baseRoleOf(role)==="Floor Supervisor"` — `can()` reads only MATRIX[role], so CUSTOM roles based on Floor Supervisor were silently denied everywhere (this was Charbel's lockout alongside (2)). (2) PATCH /api/operations/[id] FS machine branch demanded `currentOp.machineId` to exist ("a Manager must assign the first one" 409) and compared categories against that machine — a step with NO machine was unassignable by anyone but Manager. Now: validates the TARGET machine (exists; not Maintenance/Offline) and enforces same-category ONLY when a machine is already assigned (the category lives on the machine row — order_operations has NO machineCategory column and migrations are banned; orders/[id] computes machineCategory via its join). Work-started 409 and reception-approval 409 unchanged. All three assign-machine gates now use canAssignMachines. (3) GET /api/bom candidates: same-category equivalents when assigned; ALL Active/In-Use machines when not (so the Reception board offers rows for never-assigned steps); ops select returns machineCategory (machines.category). Reception board assignment section gate canApprove→canAssign and shows steps with >=1 candidate (was >1, hiding single-machine categories like a lone edge bander). (4) OrderWorkflowDetail "Station:" dropdown shows ONLY machines of the step's category (+ the current one as legacy fallback); interactive only for canAssignMachines roles — others get a disabled select with a tooltip; handleSwapMachine surfaces server 409/403 text (same-category / reception / started) via the action-error strip instead of dying in the console. render-test 198→204 (canAssignMachines: Manager/FS/FS-based custom true, operator/warehouse/undefined false; custom roles registered then restored).

- **Build stamp in Settings** (added 2026-09-15): scripts/build-prod.cjs injects the current git short SHA as NEXT_PUBLIC_WOODTEK_BUILD when running `next build`; Settings (Manager only) shows "Build: <sha>" at the bottom so anyone can verify which version a PC is actually running after update-woodtek.bat. Unknown/dev fallback when not built through build-prod. (Added after the tenth bundle: the machine-assignment fix was reported broken while the shop PC most likely had not pulled it — now verifiable at a glance.)

- **Floor Supervisor machine-list scoping fix** (fixed 2026-09-15, eleventh bundle — the REAL root cause of Charbel's lockout, found via screenshot): dataAccess.ts `allowedMachineIdsSubquery` returned ALL machines for Manager/Technician, own machines for operators, and a deny-all sentinel (id=-1 → ZERO rows) for everyone else — Floor Supervisor (and any FS-based custom role) fell into that deny branch, so /api/machines returned an empty list and the Station dropdown on the order workflow screen had nothing to offer (screenshot showed only "Unassigned"). Fix: FS and baseRoleOf(FS) customs now get the full list (return undefined = unscoped), matching the order-scoping fix from 2026-09-11 — machine scoping and order scoping are separate subqueries and BOTH must list every non-Manager role that needs visibility. Reception-board candidates were never affected (computed server-side from an unscoped list in /api/bom). Lesson recorded: when a dropdown is empty for one role but full for Manager, check allowedMachineIdsSubquery/allowedOrderIdsSubquery first. No render-test addition (DB-bound scoping); TSC + build verified; verifiable live via Build stamp ced9db1+.

- **Crew job lock + dashboard refresh after delivery** (added 2026-09-15, twelfth bundle — user requests): (1) **Crew job lock** — on multi-operator machines the crew member who STARTED a running job owns its controls: Station Mode greys START / FINISH & PASS NEXT / REJECT-REWORK (existing disabled:opacity-40) for the other crew members and shows a "Run by <operator> — view only" chip; the card stays fully visible. Pure rule src/lib/jobLock.ts `jobLockedByOther(op, viewerId, viewerIsOperatorBased)`: locks only when status==="In Progress" AND operatorId recorded AND different from viewer; operators never locked on Ready/queued jobs (anyone may take the next one) and a job with NO recorded starter stays take-over-able (fallback). OperatorStationView computes operatorBasedViewer = baseRoleOf(role)==="Machine Operator" (customs included; Manager/FS/QA unaffected). Server mirror in PATCH /api/operations/[id]: Machine-Operator-based user sending a status change on an In-Progress op with a different recorded operatorId → 403 "This job is being run by <name> — view only for you." (users.name joined for the message; operators already self-attribute operatorId on every touch action). (2) **Dashboard refresh after delivery** — ScheduleView confirmDelivery and setDispatchStage now call onRefresh() (= page fetchAllData) after their PUTs, so the dashboard KPIs/status counts (fed by the /api/dashboard prop, fetched only in fetchAllData) update as soon as an order is marked delivered — previously the numbers stayed stale until a full browser refresh. render-test 204→210 (jobLock matrix: mate locked, starter free, supervisors free, queued free, no-starter free, string-id coercion).

- **Per-material production stage (cut-list tracking)** (added 2026-09-15, thirteenth bundle — user request: one order, two materials with separate cutting lists, show which is cut and which moved to edge banding): each BOM material line of an order now carries a PRODUCTION STAGE — "" (Not started), any of the order's own operation step names (e.g. "Beam Saw Cutting", "Edge Banding" — options built from order.operations sorted by stepOrder), or DONE. Storage data/material-progress.json {version:1, progress:{"<orderMaterialsId>":{stage,at,by}}} (no migration); pure src/lib/materialProgress.ts (sanitizeStage trim/cap 40, sanitizeProgressMap numeric-keyed strict entries, stageDisplay → label+tone slate/amber/emerald); GET/PUT /api/material-progress (GET ?orderId filters to that order's lines, orders:read; PUT gated operations:update-status OR quality:write OR orders:write OR inventory:write — so operators can set it at the floor; validates the line exists, audited material.stage). UI: order Workflow tab → BOM Materials — every material line shows a stage chip (Not started slate / "→ Edge Banding" amber / "✓ Done" emerald) next to Consumed/Released, plus a compact select (Not started / the order's steps / ✓ Done) for canSetStage roles; PUT response updates local state instantly. So: material 1 stays "→ Beam Saw Cutting" while material 2 shows "→ Edge Banding" or "✓ Done". Chips are per LINE — operations/step completion remains per ORDER step (unchanged). render-test 210→216 (stage sanitize/caps/map junk rejection/display tones).

- **Material stage ordering enforced** (fixed 2026-09-15, fourteenth bundle — user caught it: a material could be manually set to "Edge Banding" while Cutting never happened): material stages now walk an ORDERED LADDER — Not started → step 1 → … → step N → DONE — one move at a time. Pure src/lib/materialProgress.ts `stageLadder(steps)` + `allowedStages(steps, current)` (returns stay / one-back / one-forward; an UNKNOWN current stage — renamed step or hand-edited file — may go anywhere so it stays correctable; an order with no steps goes "" ↔ DONE). Server: PUT /api/material-progress loads the order's operations (sorted by stepOrder), computes the ladder from the line's CURRENT stored stage, and 409s skips with "Materials move one stage at a time. From X you may go to: Y or Z." UI: the order's stage dropdown offers ONLY allowedStages(...) (current included), so illegal jumps aren't even selectable; the 409 text surfaces via the action-error strip for stale screens. Consequence to remember: a material that legitimately skips a step (e.g. hardware that never touches the edge bander) must be stepped THROUGH the intermediate stage one click at a time — a Manager-side skip/override can be added later if the shop asks. render-test 216→223 (ladder: entry/mid/last/done/unknown/no-steps + no DONE jump from the middle).

- **Material stages now FLOW with the machine (operator-driven, mostly automatic)** (added 2026-09-15, fifteenth bundle — user: manual setting "not professional", the stage should follow when the operator starts the job on the assigned machine, showing the process per material, e.g. cutting): (1) AUTO-ADVANCE — PATCH /api/operations/[id] now, AFTER a successful transition: In Progress → autoAdvanceMaterialsForStep(orderId, opName, "start") pulls every material line still BEFORE this step in the ladder INTO the step's stage ("→ Beam Saw Cutting" = being cut now); Completed → mode "complete" pushes every line AT this step's stage one further (next step, or DONE after the last). Pure logic computeAutoAdvance(lines, ladder, opName, mode) + ladderIndex in src/lib/materialProgress.ts; DB wrapper src/lib/materialProgress.server.ts (readAllProgress/writeAllProgress moved there; auto-advance NEVER throws — warn only, production first). Lines already ahead / DONE / on a custom stage are untouched. (2) STATION SHOWS THE PROCESS — GET /api/operations attaches per-op `materials` (orderMaterials⨝inventoryItems name/sku/unit/qty + live stage) for its order; Operator Station job card gained a "Materials on this step — live progress" panel: per material sku·name×qty + chip Waiting (slate) / → <step> (amber, being worked) / ✓ Done (emerald), and while the job is running a one-tap "✓ Finished here" button per material (hidden for crew-locked operators) — PUT /api/material-progress {orderMaterialsId, advance:true}: the SERVER picks the next ladder position (client cannot send a wrong/skip stage; unknown current or last stage → 409). Manual dropdown on the order BOM tab remains (validated by the same ladder) for corrections. Caveat: stages key on operation NAME — two steps with the SAME name share a ladder slot (their step names are distinct in practice; noted). render-test 223→229 (start pulls/leaves ahead-done-custom, complete pushes one/last-step→DONE, unknown op no-op).

- **Per-material flow corrected — START no longer moves every material** (fixed 2026-09-15, sixteenth bundle — user: "pressing start starts both materials at the same time, that is wrong"): the fifteenth bundle's bulk auto-advance is REMOVED (computeAutoAdvance/ladderIndex deleted from the pure lib, autoAdvanceMaterialsForStep from the server lib, calls + import from PATCH /api/operations/[id]). Stages now move ONLY when the operator taps, per material, on the station card while the job is running: "▶ Start this material" (PUT {orderMaterialsId, stage: op.operationName}) moves ONE material into the current step — the button renders only when allowedStages(orderSteps, mStage) includes the step, so a material that hasn't been through earlier steps gets no button (can't start edging before cutting); "✓ Finished here" (advance:true) moves it one further, server-picked. GET /api/operations now also returns per-op orderSteps (the order's step names in stepOrder, built from the UNfiltered ops so activeOnly views still gate correctly). advanceMaterial(stage?) surfaces server 409 text via setActionError instead of failing silently. Ladder + server validation from bundle fourteen unchanged — UI and API agree; both tap types are impossible to skip with. Consequence: pressing START alone moves nothing (correct — the operator works lists one by one); completing the job with a list forgotten on the step leaves it at the step chip where the Manager can nudge it via the order BOM dropdown. render-test back to 223 (auto-advance block removed with the feature).

- **Per-material flow made comprehensive** (fixed 2026-09-15, seventeenth bundle — user: split of execution between materials still not perfect): five gaps closed. (1) STRANDED MATERIALS — PATCH /api/operations/[id] on transition to Completed now calls autoCompleteMaterialsForStep(orderId, opName) (new, in materialProgress.server.ts): every material still sitting ON the completed step's stage is carried one stage further (next step or DONE) — a forgotten "✓ Finished here" tap can never strand a cut list; lines already tapped forward untouched; NEVER throws (warn only). START remains neutral (no bulk move — sixteenth bundle rule kept). (2) CHIPS TELL THE TRUTH — station material chips now read "Not started" / "→ <step>" (on this step) / "At <step>" (parked at an earlier/later step) / "✓ Done" instead of a generic "Waiting". (3) POSITION AT A GLANCE — every material row (operator station AND the order's BOM Materials tab) carries a thin progress bar + "n/N" counter positioned on the order's ladder (Not started → steps → DONE). (4) TRACEABILITY ON THE FLOOR — /api/operations material rows now include stageAt/stageBy; hovering a material row shows "Stage: X — by <who> · <time>". (5) ladderIndex() restored in the pure lib (needed for the bars, re-tested). render-test 223→224.

- **PER-MATERIAL MACHINE ROUTING — decided in the New Order wizard** (added 2026-09-15, eighteenth bundle — the user's architectural ask: an order's materials can each need a totally different machine routing; design the New Order section so per-material routing is chosen with a clear friendly prompt): (1) WIZARD — each BOM line in the New Order → Materials tab now has a Route button (amber dot when customized) opening an INLINE builder: "Machine routing for THIS material" — pick a machine type (from the managed machine categories) → + Add step → numbered chips (1. Beam Saw → 2. Edge Banding) with ↑ ↓ reorder and × remove, Reset (follow the order's routing). Steps are MACHINE CATEGORY names; a line with NO custom route keeps following the order's operation steps (back-compat: old orders unchanged). Submit: POST /api/orders now returns createdMaterials:[{id}] (aligned with the request bom order); the wizard PUTs /api/material-routes {orderId, routes:[{orderMaterialsId, steps}]} for customized lines only. (2) API — new GET/PUT /api/material-routes (GET ?orderId returns only that order's lines, orders:read; PUT orders:write, validates lines belong to the order, sanitizeRouteSteps (≤12 steps, ≤40 chars, collapses consecutive duplicates), empty = remove, audited material.route); storage data/material-routes.json {version:1, routes:{id:[steps]}} (no migration); pure src/lib/materialRoutes.ts (sanitizeRouteSteps/routeLadder/nextInRoute) + src/lib/materialRoutes.server.ts (readAllRoutes/writeAllRoutes). (3) EXECUTION RESPECTS THE ROUTE — material-progress PUT: advance:true and manual validation ladders are the MATERIAL'S OWN route when it has one (stage space = machine categories), else the order ops (unchanged); autoCompleteMaterialsForStep(orderId, machineId, opName) now takes the completed step's machine category and carries each material along ITS OWN route (stage === category → nextInRoute), defaulting to the ops ladder for un-routed materials; /api/operations materials rows include route (and stageAt/stageBy from before). (4) STATION — job-card material rows use mat.route for the ladder/bar and match the running step by MACHINE CATEGORY (route materials) or operation name (default); "▶ Start this material" targets stepStage accordingly; the order's BOM tab shows each routed material's path inline ("Beam Saw → Edge Banding") and its stage dropdown/bar follow the same private ladder. render-test 224→229 (sanitize/ladder/next/ladderIndex). NOTE: a material's route steps are categories, so an order step whose machine is category "Edge Banding" satisfies a route step "Edge Banding" regardless of the op's display name.

- **Routing + START made visible and per-material on the floor** (added 2026-09-15, nineteenth bundle — user: routing not visible enough in the wizard and on the station, and the big START should be per material line): (1) WIZARD — every material line in New Order → Materials now shows an ALWAYS-VISIBLE route line under it: "Route: follows the order's routing — tap to give this material its own machine path" (grey) or "Route: Beam Saw → Edge Banding — tap to edit" (amber chip-style); tapping toggles the builder (the small Route icon button stays). (2) ORDER SCREEN — the routed material's path is now amber and prefixed "Route: …". (3) STATION — each material card shows ITS OWN machine path as step chips with the live position highlighted (✓ green = passed, amber = this machine now, grey = ahead) + n/N; BIG per-material buttons replace the small ones: START (amber, full-width) starts the machine job AUTOMATICALLY if it is not running yet (PATCH op In Progress with the operator; a 409 — e.g. previous step incomplete — aborts and shows the reason) then moves that one material into the step; FINISH (emerald, full-width) sends the material to its next stage and, when the LAST material leaves the step, the machine job COMPLETES ITSELF (handleTouchAction Completed → next station unlocks, stats recorded). The job-level START MACHINING / FINISH & PASS NEXT buttons now appear ONLY for jobs with no materials (unchanged there); with materials the right column shows a hint card "Start / finish each material below — the machine job opens and closes by itself". REJECT/REWORK stays job-level. New handlers startMaterial/finishMaterial in OperatorStationView (finishMaterial auto-completes using pre-advance closure data: others at stepStage = materials minus this one still at the step). Crew lock greys the line buttons and shows "Run by another operator" in place of the buttons. render-test 229 (unchanged — UI-only bundle; TSC + build verified).

- **Station materials panel enlarged + JSX escape bug fixed** (fixed 2026-09-16, twentieth bundle — user screenshot): (1) SCREENSHOT BUG — JSX TEXT nodes and quoted JSX attributes do NOT process \uXXXX escapes (only JS expression containers do), so the station showed literal "\u2014", "\u25b6", "\u00b7", "\u00d7" strings on the hint card, the header hint and inside material names. RULE: emit real Unicode characters in JSX text/attributes; escapes only inside {"…"}. All station text fixed (hint card, header hint, name row separators, button titles). (2) LAYOUT — the materials panel was squeezed inside the narrow job-details column (screenshot: ~200px column, huge empty space). It now renders FULL-WIDTH below the card's header row (its own border-t section, "Materials — start & finish each cut list" + "the machine job opens and closes by itself" header) as a responsive grid (1/2/3 columns at sm/2xl) of bigger material cards: text-sm names, bigger stage chip, route step chips, BIG full-width START (amber) / FINISH (emerald) buttons px-6 py-4 text-base, thicker progress bar. The old per-line buttons and narrow panel are gone; job-level START/FINISH remains only for material-less jobs; REJECT/REWORK and the crew-lock chip stay in the action column. Panel relocation was line-based: panel span = from "{(op.materials?.length ?? 0) > 0 && (" to the "})}" whose next 3 lines are "</div></div>)})}" (the inner route-chips map ALSO closes with "})}" — first-match is wrong, match on the closing pattern). render-test 229 (UI-only), TSC + build verified.

- **Station hint card wrapped, left area enlarged** (fixed 2026-09-16, user screenshot follow-up): the "Start / finish each material below — the machine job opens and closes by itself" hint card stretched full-row; now max-w-sm + text-sm + py-5 + leading-relaxed + text-center so it wraps onto ~3 compact lines inside the action column — the job-details column (flex-1) gets the freed width. No other changes; TSC/render-test 229/build verified.

- **Wizard redesigned materials-first: per-material routing BY RECIPE** (added 2026-09-16, twenty-first bundle — user's flow: after order info, pick materials and assign each its routing by PICKING A RECIPE): New Order tabs are now 1 · Order Details → 2 · Materials & Routing → 3 · Order Routing (default). Tab 2: each material line has its always-visible route line + tap panel that now leads with RECIPE CHIPS (every routing recipe from Routing Recipes; hover shows its machine path) — picking one instantly sets that material's route (recipeRouteSteps: the recipe's machine categories in step order, duplicates collapsed, falls back to operationName when a step has no category); "or build it by hand" custom builder stays below; Reset clears both. Deleting a material line reindexes bomRoutes AND bomRecipeSel (removeBomLine — index-keyed maps shifted silently before). Submit: routes PUT includes recipe-derived lines; when the user leaves tab 3 EMPTY, the ORDER ROUTING IS AUTO-BUILT as the union of the materials' routes in first-appearance order (op name = category, auto machine pick, 60 min each) — so picking recipes per material is enough to create a fully routed order; the tab-3 nag no longer forces routing when materials carry recipes. Tab 3 header now says "Order Routing (default) — for materials without their own recipe; leave empty to auto-build". Modal subtitle shows the 1-2-3 flow. Pure helper recipeRouteSteps in materialRoutes.ts. render-test 229→232.

- **Step-2 deadlock + per-material sequencing** (fixed 2026-09-16, twenty-second bundle — user screenshot + report): (1) DEADLOCK — after the auto-close of a finished step, the NEXT step's job sat READY with materials already AT it ("→ Edge Banding"): FINISH required isRunning, START required NOT atThisStep → no buttons at all (screenshot). Fix: START now also shows for materials at-this-step when the job is not running — `(canStartMat || (atThisStep && !isRunning)) && !lockedByMate` — startMaterial opens the job then re-asserts the stage (same-stage PUT is an allowed ladder "stay"). (2) SEQUENCING — PATCH operations/[id] In Progress enforced "every previous operation Completed" for the WHOLE order, so finishing material 2 at step 1 first did not unlock step 2 until material 1 also finished. Now: when the order HAS material lines, the predecessor rule is satisfied once at least ONE material has reached (or passed) this step on its own ladder (route ladder vs op ladder; target = machine category for routed lines, op name for default lines; stage "" counts as not ready) — the material's stage proves ITS predecessor work is done. Material-less orders keep the strict sequential rule. Combined with the auto job-close this gives true parallel per-material flow: cut list 2 can be edged while cut list 1 is still being cut. render-test 232 (server-side change; TSC + build verified).


- **New Order v2 — named material batches with truly independent routes** (added 2026-09-17, twenty-third bundle — user rejected the old materials-first/category-union workflow): the New Order modal was replaced by a wide four-stage **Production Order Builder**: Order → Default route → Material jobs → Review. One material row now means one named batch/cut list + one inventory allocation + one complete operation chain. A row may inherit the order default, snapshot a routing recipe, or copy/edit a custom route; full operation names, machine categories, estimates, order, and repeated visits to the same machine type are preserved. Material kits, project/machine category management, default-route recipe saving, cloning, stock warnings, assignment warnings, and one final checked submission remain available. The client no longer performs a second best-effort routing request.
- **Production-plan execution model and consistency guards** (same bundle): `src/lib/productionPlan.ts` defines bounded snapshots and position-prefixed stage keys; `src/lib/productionPlan.server.ts` stores operation/material bindings atomically in `data/production-plans.json` (no migration). `POST /api/orders` validates the whole payload, creates the order + one BOM row + one private operation chain per batch in a DB transaction, limits automatic assignment to active matching-category machines (or leaves the pass visibly unassigned), and fails rather than returning a route-less order. Start/readiness/completion/rework, material progress, auto-scheduling, assignment, and downstream resets now follow only the owning material chain; reopening an earlier pass resets that batch's downstream passes, never another batch. Planned stages cannot be advanced through the legacy progress/route APIs, loose passes and individual planned-pass deletion are blocked, linked BOM rows cannot be edited separately, and first-pass reassignment stays category-safe while synchronizing the plan and warehouse destination. Operator Station shows one named material job per operation; Order Detail groups the independent chains and supports safe cloning; Warehouse and Reception show the batch name, underlying stock, recipe, and intended machine. Existing pre-v2 orders retain their shared-route behavior, including the corrected early-pipeline and completion checks. There is intentionally no in-place v2 plan editor yet: clone/recreate an unstarted order to change its material plan.
- **Verification for bundle 23:** `npm install --no-audit --no-fund` succeeded (454 packages); `git diff --check` clean; `tsc --noEmit` clean; `node render-test.js` **250/250 ALL PASS**; standalone production build completed, type-checked, and generated all 47 pages. The only warning is the pre-existing broad NFT trace through `next.config.ts` → `src/app/api/admin/dbtools/route.ts`. Rare overlay caveat: the plan is written before DB commit so a later commit failure could leave an orphan entry; initial warehouse-target/status writes are best-effort and the BOM GET path falls back to the plan's first-pass machine.

- **System performance / Operator Station fast path** (added 2026-09-17, twenty-fourth bundle — user reported both server waiting and choppy UI on Start/Finish and queue actions): `/api/operations` now applies the existing deny-by-default order scope plus machine/active-status filters in SQL instead of selecting every operation and filtering in JavaScript. Station Mode asks for `station=true`; that one scoped response carries only the visible orders' material stages, BOM fulfilment, and reception state, so Operator Station no longer polls the complete `/api/bom` factory board. Downtime and queue requests run in parallel. Ten-second polling is overlap-safe, ignores hidden tabs, rejects stale machine responses, and fingerprints unchanged data so idle polls do not rerender every job card. Each running job now owns one live timer instead of two. Successful actions optimistically update the touched card, refetch the queue once, and refresh surrounding machine counts in the background; repeated refresh calls are coalesced with one trailing refresh. Legacy per-material Start/Finish paths no longer refetch the same queue twice.
- **Faster action transaction and supporting APIs** (same bundle): the operation PATCH keeps all predecessor, material-reception, category, one-job-per-machine, rework-chain, crew-lock, and permission checks, but uses the v2 binding to prove its BOM row, validates machine + competing run in one query, removes a redundant order update, and uses one compact order-operation snapshot instead of two full reads plus a separate parent read. `/api/orders` now queries only authorized order IDs and groups operations once instead of O(orders × all operations). `/api/machines` filters open work in SQL, groups once, reads the crew overlay once, and supports `summary=true` to omit duplicated queued-job payloads. Hot JSON overlays (production plans, BOM/reception, material progress/routes, and machine crews) now cache sanitized snapshots by path/mtime/size; writes refresh the cache, and BOM/plan writes remain safe snapshots.
- **Whole-client load and refresh fixes** (same bundle): every large workspace is a `next/dynamic` chunk and does not render while signed out; the 58 KB New Order wizard is loaded only when opened. Session/menu/role bootstrap reads start in parallel. The built-in locked Machine Operator login loads only roster + compact machine summaries before Station Mode (no dashboard, orders list, customers, templates, or inventory administration payloads). Explicit screen actions now refresh only datasets they can change instead of calling the old seven-endpoint `fetchAllData`; dashboard data is marked dirty and refreshed only when the dashboard is next opened. Manager full access, the explicit Machine Operator screen list, private material chains, repeated passes, category-safe assignment, and both data-access subqueries remain unchanged. No migration was added.
- **Verification for bundle 24:** `npm install --no-audit --no-fund` succeeded (454 packages); overlay cache read/write smoke test passed; `git diff --check` clean; `tsc --noEmit` clean; `node render-test.js` **259/259 ALL PASS**; standalone production build completed, type-checked, and generated all 47 pages. Build output confirms separate Station and New Order chunks. The only production-build warning remains the pre-existing broad NFT trace through `next.config.ts` → `src/app/api/admin/dbtools/route.ts`. Owner should hard-refresh and test both Manager and Machine Operator Start/Finish/reception/downtime flows, then confirm the Settings Build stamp changed.

## Gotchas learned the hard way
1. Build-before-End ⇒ EBUSY (server holds `.next\standalone`).
2. SYSTEM task ⇒ taskkill denied; use `schtasks /End|Run`.
3. `git init` only inside the project folder (a stray repo once landed in `C:\Windows\System32`).
4. Old Aug-19 configs lacked `standalone` → silent no-standalone builds.
5. Port busy ⇒ launcher defers with a message; that message is success, not failure.
6. The zip contained a stale nested `src/src/` copy (ignored in git); the live tree is top-level `src/`.
7. PowerShell: `rmdir /s /q` is cmd syntax; use `Remove-Item -Recurse -Force`.
8. An old untracked `update-woodtek.bat` (from the zip era) can block pulls; delete it once — the repo ships the real one.
10. `schtasks /End|Run` on the SYSTEM task needs an ELEVATED shell (`Access is denied` otherwise); `update-woodtek.bat` self-elevates via UAC.
11. `schtasks /End` stops only the supervisor (start-prod.cjs) — the spawned server child can survive as an ORPHAN holding port 3000. `build-prod.cjs` now self-heals (End task → netstat → taskkill the listener); the bat also kills leftovers after /End; start-prod.cjs kills its child on any exit.
9. Repo config files (`tsconfig.json`, `next.config.ts`, `next-env.d.ts`) can get dirty locally and block pulls; `update-woodtek.bat` now runs `git checkout --` on them before every pull — repo version always wins.

## How the agent verified changes (rebuild if needed)
- `npm install && npm run typecheck` (`tsc --noEmit`) in a clone — the project's own check.
- A `render-test.js` SSR harness (typescript `transpileModule` + `react-dom/server`) rendered the real `Sidebar` per role and asserted hidden/visible labels (59 checks). Needs `typescript`, `react`, `react-dom`, `lucide-react` from the repo's package.json.
