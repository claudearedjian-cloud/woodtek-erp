# WoodTek ERP — Handoff for a New Chat / Agent Session

_Last updated 2026-09-08. Read this first. The owner is NOT a developer — give exact copy-paste commands._

## What this project is
- Next.js **16.2.6** (standalone output) + PostgreSQL factory ERP ("WoodTek ERP – Furniture Service Center").
- Runs on a **Windows 10 PC**, folder `C:\woodtek-erp\woodtek-erp`; serves `0.0.0.0:3000`; LAN URL `http://192.168.220.179:3000`.
- Roles: Manager (Claude Aredjian), Machine Operator (maroun rahme), Sales Coordinator, QA & Dispatch, Technician.

## Source of truth
- **Private GitHub repo: `https://github.com/claudearedjian-cloud/woodtek-erp` (branch `main`).**
- The PC folder is a clone of it. Before any git work on the PC, check `git remote -v` points there.
- Other repos (`erp`, `system2`, `executable`) are old/other-session — **never** push or reset WoodTek code from them.
- To give an agent write access: create a **fresh fine-grained PAT** (only `woodtek-erp`, permission *Contents: Read and write*, short expiry), paste it in chat, revoke afterwards. Chat attachments accept images/text/md/json/pdf — **not .zip** (workaround: `copy x.zip x.txt` then attach the .txt).

## Deploy ritual (every change)
Agent commits + pushes to `main`; then on the PC, **in this exact order**:
```
git pull
node scripts\build-prod.cjs
schtasks /End /TN "\WoodTek ERP"
schtasks /Run /TN "\WoodTek ERP"
```
- **Never build while the task is running** → `EBUSY ... rmdir .next\standalone`.
- The task runs **as SYSTEM** → `taskkill` is always *Access denied*; only `schtasks /End` / `/Run` control it.

## Runtime architecture
- Scheduled task `\WoodTek ERP` (runs at logon) → `start-woodtek-prod-silent.bat` → `node start-prod.cjs --no-browser` → `node .next/standalone/server.js`. Logs: `logs\woodtek.log`.
- `start-prod.cjs` is the **v2 watchdog**: restarts real crashes (max 6/min), on `EADDRINUSE` it does NOT loop — it prints the owning PID and defers; health probe trusts only its own child process.
- `start-woodtek-prod.bat` = interactive launcher; if the task already serves port 3000 it prints "already in use … not starting a second copy" (by design — close the window).
- `next.config.ts` MUST keep `output: "standalone"` and `serverExternalPackages: ["bcryptjs","pg","@types/bcryptjs"]` — without it the build produces no `.next/standalone` and the server FATALs.
- `.env` (DATABASE_URL, AUTH_SECRET, …) is **local only**: never commit, never send to chats.

## Backups
- Task `\WoodTek ERP Backup` daily 02:30 → `backup\woodtek_factory_*.dump` (also stored in the private repo).
- `woodtek-snapshot.bat` → `backups\snapshot-DATE.zip` (source+configs+.env) before manual tinkering.
- `RUNBOOK.txt` at repo root = one-page ops guide.

## Role / UI rules implemented 2026-09-07/08
- Access matrix: `src/lib/moduleAccess.ts`; enforced by `src/components/Sidebar.tsx` (rendering) and `src/app/page.tsx` (tab guard bounces to first allowed module).
- **Machine Operator** sees only: Operator Station Mode, Scrap & Rework, Workforce & Shifts (+ Auto Workflow Engine card). *Active Role Persona* panel hidden; "Switch profile (PIN required)" kept.
- **Manager**: Routing Recipes, Downtime Log, Workforce & Shifts, PIMS Import, Shop Floor Monitor are nested **under General Settings** (indented sub-menu).
- All other roles unchanged.
- **Menu Designer** (added 2026-09-08): Manager-only top-level tab (`designer` module) to rename/reorder/regroup items and toggle per-role visibility. Stored as JSON at `<project>/data/menu-config.json` (env `WOODTEK_DATA_DIR` set by start-prod.cjs; API `/api/menu-config`, PUT/DELETE require `users:manage`). Defaults live in `src/lib/menuConfig.ts` (`MENU_REGISTRY`); `resolveMenu()` merges defaults + config per role; Sidebar renders its output. Reset button deletes the file.

- **Custom Roles** (added 2026-09-08): Manager can add/remove roles in Settings > Users > "Manage roles" (panel inside the add/edit-user form). A custom role is a **named alias of a built-in role** ("inherits ..."): it gets that role's full permission set, module visibility and data scoping. Storage: `<project>/data/roles-config.json` (`{version:1,roles:[{name,base}]}`, API `/api/roles`, PUT requires `users:manage`; removing a role still assigned to users is refused with 400). Registry: `registerCustomRoles/allRoles/baseRoleOf/getCustomRoles` in `src/lib/permissions.ts` (client-safe); server reads the file via `src/lib/rolesConfig.server.ts` (`ensureRolesRegistered()` — called from auth, users/menu-config/roles routes). **Session carries the BASE role** (`getSessionUser`/login POST resolve via `baseRoleOf`) so all ~40 legacy `role === "..."` checks keep working untouched; `displayRole` carries the custom name for UI (sidebar, profile). Menu Designer shows a column per custom role (overrides keyed by custom name; visibility defaults via base). Client registry filled at bootstrap (`page.tsx` fetches `/api/roles`). Built-in roles cannot be removed or shadowed; name <=30 chars, max 20 custom roles.

- **Machine Categories + Auto-assign** (added 2026-09-08): managers/technicians edit category names (add/remove) via "Manage categories" inside the New Order modal (also drives the Machine Type dropdown in Machines view and step categories in Recipe Manager). Storage: `<project>/data/machine-categories.json` (`{version:1,categories:[names]}`, API `/api/machine-categories`, GET open — seeds from distinct `machines.category` when no file; PUT requires `machines:write`, refuses to drop categories still used by machines). Membership = `machines.category` column (no migration). **Order routing steps now have Manual/Auto toggle**: Manual = exact machine (as before); Auto (default for new steps and recipe loads) = pick a category, server assigns the freest machine at creation (`pickFreestMachine` in `/api/orders` POST: exact→includes category match, skips Maintenance/Offline, least open ops (Pending/Ready/In Progress), active downtime pushed to back; helpers `sanitizeCategories`/`chooseFreestMachine` in `src/lib/machineCategories.ts`). Recipes (`templateId`) and the default 2-step path also use it. render-test covers the pure helpers (59 checks).

- **Editable Project Categories** (added 2026-09-08): the "Project Category" dropdown on the New Order form is Manager-editable ("Add / remove project categories" under the select). Storage: `<project>/data/project-types.json` (`{version:1,types:[names]}`, API `/api/project-types`, GET open — defaults (`DEFAULT_PROJECT_TYPES` in `src/lib/projectTypes.ts`) merged with distinct `orders.projectType` when no file; PUT requires `users:manage`, refuses to remove types still used by orders). Dropdown also always includes values existing orders use, so nothing disappears from history.

- **Warehouse & BOM** (added 2026-09-08): BOM lines can be entered directly on the New Order form ("Materials / BOM" section: inventory item + qty; written to the existing `orderMaterials` table with unit costs — same table as the order-detail BOM tab, stock still reserved/consumed by the existing materials engine). New top-level **Warehouse & BOM** tab (`warehouse` module, granted to Manager + QA & Dispatch): `src/components/WarehouseView.tsx` lists every open order (not Completed/On Hold/Cancelled) with its BOM lines; warehouse staff move lines Requested → Prepared → Delivered and pick a target machine from the order's operations. API `/api/bom` (GET orders:read; PUT requires inventory:write OR orders:write). Fulfilment status has no DB column (no migrations): JSON overlay `data/bom-status.json` keyed by allocation id (`src/lib/bomStatus.server.ts`). **Warehouse Supervisor** = custom role inheriting **QA & Dispatch** (create in Settings → Users → Manage roles). render-test: 63 checks. V2: custom roles may carry a `modules` allowlist (role editor in Settings > Users > Manage roles: pencil per role or checkboxes on add; empty = full base access) which REPLACES base screens in canAccessModule/listModulesForRole (page guard uses displayRole); BOM lines auto-target the FIRST machine of the routing (overlay set at creation), warehouse staff see it read-only and only `users:manage` (Manager) can re-route (PUT /api/bom). V3: operator station shows each order's BOM with line statuses and big RECEIVED / NOT RECEIVED confirmation buttons (PUT /api/bom {orderId, received}, allowed via inventory:write which operators have); the confirmation appears as chips on the Warehouse board order headers so manager/warehouse see who is waiting. V4: Dispatch queue on the Dispatch Schedule view: Completed orders appear with a stage pipeline — project categories whose name contains 'service' go straight to Awaiting delivery, everything else follows Cleaning -> QC -> Packing -> Awaiting delivery -> Delivered; stage advancement via PUT /api/dispatch (quality:write OR orders:write OR inventory:write); stages stored in data/dispatch-status.json (src/lib/dispatch.ts pure helpers). render-test 68 checks. V5: Delivery closes the loop — "Mark delivered" in the Dispatch queue sets orders.status="Delivered" in the DB (PUT /api/dispatch); Orders & Routing gains a Delivered filter chip, a teal Delivered kanban column + badge, and live "Dispatch: <stage>" chips on completed orders (polls /api/dispatch every 15 s); Delivered orders leave every open-work query (WIP materials, BOM board, customer active counts) and count as Completed in dashboard/reports stats; order materials lock treats Delivered as terminal. render-test 68 checks.
- **Dashboard rearrange** (added 2026-09-09): the OEE & Plant Performance panel moved off the Executive Dashboard into its own **Plant Performance** tab (`plant` module, `src/components/PlantView.tsx`, Gauge icon, granted to Manager / Sales Coordinator / QA & Dispatch / Technician; fetches /api/dashboard?window= itself). The WIP Pipeline Value and Machine Utilization KPI tiles were removed from the dashboard top. The dashboard now opens with an **Orders by status** quick bar (In Production / Pending / Quality Review / Completed / Delivered / On Hold, live counts from orderStatusDistribution) — each button jumps to Orders & Routing pre-filtered (page.tsx `navigateWithStatus` + OrdersView `presetStatus` prop). orderStatusDistribution now reports Completed and Delivered separately (+Cancelled). Order numbers are rendered bigger/bolder (font-mono font-black amber) with the client name next to them on the dashboard feed, Orders list + kanban, WIP board (running card + queue chips + order cards), Warehouse board, Dispatch queue, Operator Station pill and Machines running card; /api/dashboard activeShopJobs and /api/wip currentJob now carry customerName.
- **CMMS full access** (added 2026-09-09): Power & Generator telemetry cards now carry **Edit Asset** (full field modal: tag/name/brand/type/site/serial/series/year/kVA/meter/interval/last-service/status/criticality/notes → POST /api/cmms with id) and **Delete** (confirm → DELETE /api/cmms?assetId=) buttons for Manager/Technician/QA & Dispatch; canManage in CmmsView + AssetDetailView extended to QA & Dispatch; asset DELETE now guarded by cmms:write (was machines:write, which locked out QA & Dispatch). Created the MISSING `src/app/api/cmms/logs/route.ts` — CmmsView and AssetDetailView were POSTing maintenance events to a 404; POST inserts the log (resets service meter + moves hour meter forward when resetService), DELETE ?logId= removes an entry (trash icon per log row in AssetDetailView).
- **Project categories deletable** (added 2026-09-09): OrdersView used to re-merge DEFAULT_PROJECT_TYPES into the list on load and after every save, so deleted default categories (Custom Kitchens etc.) instantly reappeared. Now the client trusts the stored list verbatim (GET /api/project-types already seeds defaults+in-use types when no data/project-types.json exists). The server still refuses (400, message shown in the manager box) to remove a category that existing orders carry — by design; force-delete can be added if the boss asks.
- **Sequential order numbers** (added 2026-09-09): new orders are numbered **PO-0001/2026** — 4-digit sequential counter scoped to the calendar year (`src/lib/orderNumbers.server.ts` nextOrderNumber(), used by POST /api/orders when no explicit orderNumber is supplied; the old fallback was a random ORD-2026-XXXX with a hardcoded year). The counter finds MAX(PO-####/<year>) in the orders table, so it auto-resets to 0001 on Jan 1 with zero maintenance. Legacy ORD-…/PIMS-… numbers are untouched and skipped by the pattern.
- **New Order modal = 3 tabs** (added 2026-09-09): the crowded one-scroll form is now a wizard — `1 · Order Details` (client, project category + manager, title, priority, value, due date), `2 · Machine Routing` (recipes + step chain), `3 · Materials (n)` (BOM). Back/Next buttons in the footer; Schedule & Route Order submits from any tab; the form onSubmit jumps to the offending tab when validation fails (missing client/title → Details, no steps → Routing); tab resets to 1 whenever the modal opens.
- **Auto-plan dispatch slots** (added 2026-09-09): the "Needs a slot" panel on Dispatch Schedule has a big **Auto-plan N operations** button (Manager/Sales). `POST /api/operations/auto-schedule` (operations:update-status) assigns slots to every Pending/Ready operation without one: orders sorted Urgent→High→Normal→Low then due date; inside an order operations follow stepOrder and never start before the previous step's end; machines are never double-booked (existing bookings respected, incl. slots assigned in the same run) and Maintenance/Offline machines are skipped; operations without a machine go to whichever usable machine can start them earliest. Manual "Plan slot" modal remains for fine-tuning; response reports planned/skipped counts.
- **Order Status report fixed** (added 2026-09-10): the report generated fine server-side (byStatus counts + full order list) and PDF/CSV exporters existed, but ReportContent had NO "Order Status" branch, so the on-screen preview fell through to "Report type not recognized." — added the renderer: 5 status stat cards (Pending / In Production / Quality Review / Completed / On Hold, Completed includes Delivered) + full orders table (order #, title, status chip, progress, due date, value).
- **Clients & Architects overhaul** (added 2026-09-10): CustomersView rewritten — top-bar **search** (company/contact/email/phone/address, live "Showing X of Y", clear button, empty state); clicking any client card opens a **Client File modal** with two tabs: **Profile** (all info incl. credit limit, balance, notes; **Edit Information** inline form → PATCH /api/customers/:id; **Delete Client** → DELETE, server still blocks deletion while orders exist) and **Orders (n)** (full order history from GET /api/customers/:id — big amber order numbers, status chips, progress, due date, value; row click jumps to the order workflow). Card trash icon still works with stopPropagation.

## Gotchas learned the hard way
1. Build-before-End ⇒ EBUSY (server holds `.next\standalone`).
2. SYSTEM task ⇒ taskkill denied; use `schtasks /End|Run`.
3. `git init` only inside the project folder (a stray repo once landed in `C:\Windows\System32`).
4. Old Aug-19 configs lacked `standalone` → silent no-standalone builds.
5. Port busy ⇒ launcher defers with a message; that message is success, not failure.
6. The zip contained a stale nested `src/src/` copy (ignored in git); the live tree is top-level `src/`.
7. PowerShell: `rmdir /s /q` is cmd syntax; use `Remove-Item -Recurse -Force`.
8. An old untracked `update-woodtek.bat` (from the zip era) can block pulls; delete it once — the repo ships the real one.
10. `schtasks /End|Run` on the SYSTEM task needs an ELEVATED shell (`Access is denied` otherwise); `update-woodtek.bat` self-elevates via UAC.
11. `schtasks /End` stops only the supervisor (start-prod.cjs) — the spawned server child can survive as an ORPHAN holding port 3000. `build-prod.cjs` now self-heals (End task → netstat → taskkill the listener); the bat also kills leftovers after /End; start-prod.cjs kills its child on any exit.
9. Repo config files (`tsconfig.json`, `next.config.ts`, `next-env.d.ts`) can get dirty locally and block pulls; `update-woodtek.bat` now runs `git checkout --` on them before every pull — repo version always wins.

## How the agent verified changes (rebuild if needed)
- `npm install && npm run typecheck` (`tsc --noEmit`) in a clone — the project's own check.
- A `render-test.js` SSR harness (typescript `transpileModule` + `react-dom/server`) rendered the real `Sidebar` per role and asserted hidden/visible labels (59 checks). Needs `typescript`, `react`, `react-dom`, `lucide-react` from the repo's package.json.
